server:
  port: 9001

spring:
  application:
    name: feign-consumer

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/

feign:
  hystrix:
    enabled: true